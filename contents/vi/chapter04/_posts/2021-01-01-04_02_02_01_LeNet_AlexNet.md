---
layout: post
title: 04-02-02-01 LeNet và AlexNet
chapter: '04'
order: 12
owner: Deep Learning Course
lang: vi
categories:
- chapter04
---

## 4. Kiến trúc CNN Kinh điển

Sự tiến hóa của kiến trúc CNN là hành trình hấp dẫn của việc tăng độ sâu, hiệu quả và các lựa chọn thiết kế thông minh. Hãy lần theo tiến hóa này chi tiết.


![CNN](/deep-learning-self-learning/img/chapter_img/chapter04/conv.jpg)
*Tổng quan kiến trúc Mạng Neuron Tích chập (CNN). Nguồn: AnalyticsVidhya*

![CNN](/deep-learning-self-learning/img/chapter_img/chapter04/conv6.jpg)
*Tổng quan kiến trúc Mạng Neuron Tích chập (CNN). Nguồn: AnalyticsVidhya*

### LeNet-5 (1998) — Người Tiên phong

LeNet-5 của Yann LeCun là CNN thành công đầu tiên, được thiết kế cho nhận diện chữ số viết tay.

**Kiến trúc**:
```
Input (32×32×1)
    ↓
Conv1: 6 filters, 5×5, stride 1 → 28×28×6
    ↓
AvgPool: 2×2, stride 2 → 14×14×6
    ↓
Conv2: 16 filters, 5×5, stride 1 → 10×10×16
    ↓
AvgPool: 2×2, stride 2 → 5×5×16
    ↓
Flatten → 400
    ↓
FC1: 400 → 120
    ↓
FC2: 120 → 84
    ↓
Output: 84 → 10
```

**Đặc điểm then chốt**:
- ~60.000 tham số
- Kích hoạt tanh/sigmoid (ReLU chưa tồn tại)
- Average pooling (max pooling xuất hiện sau)
- Huấn luyện trên MNIST (10 lớp, ảnh 28×28)

```python
class LeNet5(nn.Module):
    """Kiến trúc LeNet-5 (hiện đại hóa với ReLU)"""
    def __init__(self, num_classes=10):
        super().__init__()
        
        self.features = nn.Sequential(
            nn.Conv2d(1, 6, kernel_size=5),       # 32→28
            nn.ReLU(),
            nn.AvgPool2d(2, 2),                   # 28→14
            nn.Conv2d(6, 16, kernel_size=5),     # 14→10
            nn.ReLU(),
            nn.AvgPool2d(2, 2),                   # 10→5
        )
        
        self.classifier = nn.Sequential(
            nn.Linear(16 * 5 * 5, 120),
            nn.ReLU(),
            nn.Linear(120, 84),
            nn.ReLU(),
            nn.Linear(84, num_classes)
        )
    
    def forward(self, x):
        x = self.features(x)
        x = x.view(x.size(0), -1)
        x = self.classifier(x)
        return x
```

### AlexNet (2012) — Bước đột phá Học sâu

AlexNet của Krizhevsky, Sutskever và Hinton thắng ImageNet 2012 một cách ấn tượng và khởi động cuộc cách mạng học sâu.

**Kiến trúc**:
```
Input (227×227×3)
    ↓
Conv1: 96 filters, 11×11, stride 4 → 55×55×96
    ↓
MaxPool: 3×3, stride 2 → 27×27×96
    ↓
Conv2: 256 filters, 5×5, pad 2 → 27×27×256
    ↓
MaxPool: 3×3, stride 2 → 13×13×256
    ↓
Conv3: 384 filters, 3×3, pad 1 → 13×13×384
    ↓
Conv4: 384 filters, 3×3, pad 1 → 13×13×384
    ↓
Conv5: 256 filters, 3×3, pad 1 → 13×13×256
    ↓
MaxPool: 3×3, stride 2 → 6×6×256
    ↓
Flatten → 9216
    ↓
FC1: 9216 → 4096 (+ Dropout 0.5)
    ↓
FC2: 4096 → 4096 (+ Dropout 0.5)
    ↓
Output: 4096 → 1000
```

**Đóng góp cách mạng**:
1. **Kích hoạt ReLU**: Huấn luyện nhanh hơn tanh/sigmoid
2. **Dropout**: Chính quy hóa để chống overfitting
3. **Tăng cường dữ liệu**: Crop ngẫu nhiên, lật, biến đổi màu
4. **Huấn luyện GPU**: Chia trên 2 GPU (mỗi cái 3GB lúc đó)
5. **Local Response Normalization**: Ức chế bên (sau được thay bằng BatchNorm)

**~60 triệu tham số** — khổng lồ so với thời điểm đó!

```python
class AlexNet(nn.Module):
    """AlexNet đơn giản hóa"""
    def __init__(self, num_classes=1000):
        super().__init__()
        
        self.features = nn.Sequential(
            nn.Conv2d(3, 96, kernel_size=11, stride=4, padding=2),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=3, stride=2),
            
            nn.Conv2d(96, 256, kernel_size=5, padding=2),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=3, stride=2),
            
            nn.Conv2d(256, 384, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            
            nn.Conv2d(384, 384, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            
            nn.Conv2d(384, 256, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=3, stride=2),
        )
        
        self.classifier = nn.Sequential(
            nn.Dropout(0.5),
            nn.Linear(256 * 6 * 6, 4096),
            nn.ReLU(inplace=True),
            nn.Dropout(0.5),
            nn.Linear(4096, 4096),
            nn.ReLU(inplace=True),
            nn.Linear(4096, num_classes),
        )
    
    def forward(self, x):
        x = self.features(x)
        x = x.view(x.size(0), 256 * 6 * 6)
        x = self.classifier(x)
        return x
```
